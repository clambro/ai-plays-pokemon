from enum import StrEnum


class Button(StrEnum):
    """The buttons that can be pressed in the game."""

    A = "a"
    B = "b"
    SELECT = "select"
    START = "start"
    UP = "up"
    DOWN = "down"
    LEFT = "left"
    RIGHT = "right"
